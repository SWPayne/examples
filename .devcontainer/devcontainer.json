{
  "name": "DATA304",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",
  "containerEnv": { "GIT_LFS_SKIP_SMUDGE": "1" },
  "postCreateCommand": "python -m pip install --upgrade pip && pip install -r requirements.txt pytest && rm -f .git/hooks/pre-push && git config --local --remove-section filter.lfs || true",
  "customizations": {
    "vscode": { "extensions": ["ms-python.python","ms-toolsai.jupyter"] }
  },
  "remoteUser": "vscode"
}